<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projekt">

    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".layout.ChangeNotificationActivity" />
        <activity android:name=".layout.ChangeMealActivity" />
        <activity android:name=".layout.EditPasswordActivity" />
        <activity android:name=".layout.HistoryActivity" />
        <activity android:name=".layout.EditNotificationsActivity" />
        <activity android:name=".layout.EditGoalsActivity" />
        <activity android:name=".layout.RegisterActivity" />
        <activity android:name=".layout.MenuActivity" />
        <activity android:name=".layout.EditMealsActivity" />
        <activity android:name=".layout.NotificationsActivity" />
        <activity android:name=".layout.GoalActivity" />
        <activity android:name=".layout.OptionsActivity" />
        <activity android:name=".layout.StatisticsActivity" />
        <activity android:name=".layout.MealActivity" />
        <activity android:name=".layout.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".IntentServiceNotifications"/>
    </application>

</manifest>